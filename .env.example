# Used when token is not set
GITHUB_USERNAME=PRAJWAL-BR-0304

# Recommended: personal access token for reliable profile lookup.
# Create token: https://github.com/settings/tokens
# Fine-grained token with read-only metadata is enough.
GITHUB_TOKEN=
